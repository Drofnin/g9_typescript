
{
const name:string = "Mark Pascoe";
const names:string [] = ["Mark Pascoe", "Cyrus Sappy", "Drucy Vigar", "Chaunce Hanner", "Jacquie Hart"];  // Array of string, marked with brackets after'string' 
                       
type PeopleType ={                  //this is how we use TS to say its objects
    name:string;
    dob:string;                 // ??       
    children:number;
    country:string;             // ??
    KnowsHowToProgram:boolean;
}

